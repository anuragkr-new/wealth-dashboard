# Local Postgres (Docker: npm run db:up)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/wealth?schema=public"

# Railway: use the DATABASE_URL from your Postgres service (Variables tab).
