/** Hardcoded gate (replace with env / proper auth when you outgrow this). */
export const ACCESS_CODE = "5593";

export const ACCESS_COOKIE_NAME = "wd_gate";

/** Opaque value stored in httpOnly cookie after successful code entry. */
export const ACCESS_COOKIE_VALUE = "wd_granted_v1";
